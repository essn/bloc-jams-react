# Bloc Jams React

## To Run
1. Clone repository
2. Run `npm install` to install dependencies
3. Run `npm start` to run server
4. Visit `localhost:3000`

## Known Issues
Certain versions of Chrome will not play the audio files in this project.  To fix this, change your version of Chrome or try a different browser.
